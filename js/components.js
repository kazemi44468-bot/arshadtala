const AT={async init(){const slot=document.querySelector('[data-sidebar]');if(slot){try{const r=await fetch('/components/floating-sidebar.html',{cache:'no-store'});if(!r.ok)throw new Error(r.status);slot.innerHTML=await r.text()}catch(e){console.error('ArshadTala sidebar:',e);return}}this.reserveSidebar();this.initSidebar();this.markActive()},reserveSidebar(){if(document.querySelector('.floating-bar'))document.body.classList.add('has-floating-sidebar')},initSidebar(){document.querySelectorAll('.side-parent').forEach(btn=>btn.addEventListener('click',e=>{e.stopPropagation();const group=btn.closest('.side-group');document.querySelectorAll('.side-group.open').forEach(g=>{if(g!==group)g.classList.remove('open')});group.classList.toggle('open')}));document.addEventListener('click',()=>document.querySelectorAll('.side-group.open').forEach(g=>g.classList.remove('open')))},markActive(){const current=location.pathname.replace(/\/$/,'')||'/';document.querySelectorAll('.floating-bar a').forEach(a=>{const href=new URL(a.href,location.href).pathname.replace(/\/$/,'')||'/';if(href===current||current.startsWith(href+'/'))a.classList.add('active')});document.querySelectorAll('.side-group').forEach(g=>{if([...g.querySelectorAll('.side-submenu a')].some(a=>{const href=new URL(a.href,location.href).pathname.replace(/\/$/,'')||'/';return href===current||current.startsWith(href+'/')}))g.classList.add('open')})}};document.addEventListener('DOMContentLoaded',()=>AT.init());